# V0.3 Tasks & Projects Validation — `V03-TP-01`

> Validation date: 2026-08-19
>
> Branch: `agent/v0.3-tasks-projects-crud`
>
> Commit under test: `b09a483`
>
> Status: `LOCAL PASS / CLOUD AUTH SESSION REQUIRED`

This document records the real-browser release-gate evidence for the first V0.3
vertical slice. The gate remains open until authenticated Cloud CRUD,
offline/reconnect Outbox delivery, and multi-session Realtime are all proven.

## Environment

- Node.js `v24.19.0`
- npm `11.9.0`
- Next.js `16.3.0`
- Chromium `149.0.7827.0`, controlled through the Chrome DevTools Protocol
- application served at `127.0.0.1:3000`
- Supabase homologation project `rchkmaohyiehktmhxkxp`
- test fixture names contained only disposable `V03` validation data

The normal `agent-browser` daemon could not bind its required Unix socket in
the sandbox. Chromium itself ran successfully, so the same DOM, viewport,
storage, network, console, and screenshot checks were executed through CDP.

## Automated baseline

| Check | Result | Evidence |
|---|---|---|
| Clean install | `PASS` | `npm ci` installed 408 packages from the committed lockfile |
| Focused tests | `PASS` | Node test runner: 6/6 passed |
| TypeScript | `PASS` | `tsc --noEmit` exited 0 |
| ESLint | `PASS` | ESLint exited 0 |
| Tracked-file secret scan | `PASS` | 103 tracked files scanned |
| Registry signatures | `PASS` | 407 signed packages and 92 attestations verified |
| Production build | `PASS` | Next.js compiled and generated 9/9 static pages |
| Dependency audit | `FAIL` | 9 high-severity findings in the `nanoid`/`postcss` dependency chain; npm reported no fix available |

The dependency advisory appeared after the previous handoff and must not be
reported as a zero-vulnerability result. It is separate from the Tasks/Projects
runtime behavior and was not worked around by changing dependencies during this
gate.

## Local Mode browser matrix

| ID | Scenario | Expected | Observed | Status |
|---|---|---|---|---|
| L-01 | Create Project | Name, description, and deadline persist | Project rendered with `31/08/2026` and was present in `mypersonaos_state_v2_local` | `PASS` |
| L-02 | Edit Project | Changed fields and `updatedAt` persist | Name and deadline changed; UI rendered `01/09/2026` | `PASS` |
| L-03 | Create linked Tasks | Priority, project, due date, and Today flag persist | Two Tasks were linked to the Project with distinct priorities/dates | `PASS` |
| L-04 | Edit and reschedule Task | Title, status, priority, due date, and Today flag update | Task changed to `in-progress`, low priority, due `25/08/2026`, and Today | `PASS` |
| L-05 | Complete Task | Completion is immediate and timestamped | Task changed to `done` with `completedAt` and updated rendering | `PASS` |
| L-06 | Derived progress | One of two Tasks done produces 50% | Project rendered `Progresso (1/2 tarefas)` and `50%` | `PASS` |
| L-07 | Reload persistence | CRUD state survives a full reload | Tasks, dates, status, Today flags, and 50% progress survived reload | `PASS` |
| L-08 | Delete Project | Tasks remain but no dangling `projectId` exists | Project was removed; both Tasks remained with `projectId` absent | `PASS` |
| L-09 | Delete Task | Task disappears from UI and LocalStorage | Deleted Task did not return after the state write | `PASS` |
| L-10 | Mobile controls | Core controls remain visible without horizontal overflow | At `390×844`, `scrollWidth === innerWidth`; open/edit/delete controls were visible | `PASS` |
| L-11 | Runtime health | No browser/runtime errors | No Next.js overlay, exception, HTTP error, or failed request was detected | `PASS` |

The persisted Project cache contained no duplicated `Project.tasks` collection;
the global Tasks collection remained the source of truth. Calendar-only values
remained `YYYY-MM-DD` in storage and rendered on the same selected day.

## Cloud/Supabase matrix

| ID | Scenario | Observed | Status |
|---|---|---|---|
| C-01 | Project/schema health | Project is `ACTIVE_HEALTHY`; four migrations and six RLS-enabled personal tables are present | `PASS` |
| C-02 | Cloud auth boundary | Unauthenticated `/` redirected to `/login`; private shell content did not render | `PASS` |
| C-03 | Database residue | All six personal tables contained zero rows before and after this partial validation | `PASS` |
| C-04 | Authenticated login/logout | No authenticated test session was available in the clean checkout/browser profile | `BLOCKED` |
| C-05 | Authenticated Tasks/Projects CRUD | Requires C-04 | `BLOCKED` |
| O-01 | Offline create/update/delete → reload → reconnect | Requires an authenticated Cloud session and durable Outbox scope | `BLOCKED` |
| O-02 | Outbox drains to zero | Requires O-01 | `BLOCKED` |
| R-01 | Two-session/device Realtime, including DELETE | Requires two authenticated sessions for the same owner | `BLOCKED` |
| R-02 | Realtime final-state consistency after a burst | Requires R-01 | `BLOCKED` |

No credential was invented, extracted, committed, or written into this report.
The ignored local Cloud configuration contains only the project URL and browser-
safe publishable key.

## Gate decision

`V03-TP-01` remains `IN VALIDATION`.

Local Mode is proven. Cloud CRUD, Offline/Outbox, and Realtime are not failures,
but they are unproven because the environment lacks an authenticated disposable
test session. Do not start `SEC-11`, `PERF-01`, `V03-DATA-01`, or
`V03-TODAY-01` until the remaining matrix is executed.

## Next action

Provide or establish a disposable authenticated browser session for the
homologation owner, then resume at C-04 and execute C-05 → O-01/O-02 → R-01/R-02.
After the matrix passes, repeat the full automated ladder, resolve or explicitly
triage the dependency-audit failure, update the roadmap/state/handoff, and commit
the completed evidence.
